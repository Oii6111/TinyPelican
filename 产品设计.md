# 小鹈鹕AI V3 产品设计

> 版本：v0.4（新增：剪贴板自动捕获 MVP 落地、关系维护提醒、语音转写顺序回填）
> 状态：MVP 核心模块部分落地，产品方向已收敛，后续按路线图迭代

---

## 1. 一句话定位

> **小鹈鹕AI V3 是一个"长记忆 + 主动关系"的个人全能助手：无缝接入并管理用户的社交聊天记录，用 Agent 分析每一段关系，从聊天中识别任务与 DDL，主动提醒、辅助回复、推进任务，成为时刻在用户身边的第二大脑。**

### 1.1 核心优势（价值主张）

**一句话：小鹈鹕把个人 Agent 的交互成本从"用户自己编排"压缩到"复制一下就够"，把 AI 的角色从"你叫它才动"反转为"它主动帮你想到、提醒你、甚至帮你开口"。**

| # | 优势 | 一句话 | 对比现状 |
|---|------|--------|----------|
| 1 | 交互成本趋近于零 | "复制"即接入 | 传统 Agent 要整理、上传、配置、写 prompt |
| 2 | 主动性反转 | 从"人分配工作给 Agent"到"Agent 替人想事" | 传统 Agent 是等指令的劳动力 |
| 3 | 长记忆关系内核 | 不是工具，是"记得你的人" | 单次问答/单次任务，关掉即清零 |
| 4 | 无感嵌入 | 不改习惯，智能叠加在微信之上 | 换工具、换平台的高迁移成本 |
| 5 | 隐私 + 人格化 | 本地优先，小鹈鹕"吃东西"反馈 | 云端上传的信任顾虑 |

**逐条展开：**

**优势 1：交互成本趋近于零——"复制"即接入。**
用户不需要导出、迁移、上传、学习任何新操作，只需做一件已经形成肌肉记忆的事：在微信里"复制"。剪贴板自动监听、自动识别聊天格式、自动解析入库、自动提取任务——全部在后台发生，用户零学习成本、零迁移成本。

**优势 2：主动性反转——从"人分配工作给 Agent"到"Agent 主动替人想事"。**
现有 Agent 都是被动的"劳动力"，用户必须自己扮演"项目经理"：先想清楚要做什么、排优先级、再分派给 Agent。小鹈鹕把这个角色反转了——AI 自己就是项目经理，它从聊天里看到"下周交方案"就自动建档 DDL、主动提前提醒、主动建议下一步动作。用户从"想清楚了再让 AI 做"变成"AI 告诉我该做什么，我只需确认"。

**优势 3：长记忆关系内核——不是工具，是"记得你的人"。**
继承 Relationship OS（Proactive Relational Engineering）：跨会话、跨时间的长期记忆 + 关系状态机。别人做的是"单次问答"或"单次任务"，小鹈鹕做的是"持续经营关系"——记得每个人的近况、承诺、情绪变化。这也沉淀出不可复制的关系时序数据护城河。

**优势 4：无感嵌入，不改变用户习惯。**
不改动微信、不改动输入方式，作为一层智能叠加在用户已有的聊天工具之上。用户感知不到"我在用一个 AI 系统"，只感知到"有个东西在帮我记着事、提醒我、给我建议"。

**优势 5：隐私与人格化体验。**
本地优先、剪贴板只认聊天格式、非聊天内容立即丢弃——信任是个人助手的地基；小鹈鹕"吃东西"动画把数据处理人格化，是有温度的品牌记忆点。

---

## 1.2 当前已落地能力（2026-08）

| 能力 | 状态 | 说明 |
|------|------|------|
| 剪贴板自动捕获聊天记录 | ✅ 已落地 | 监听剪贴板，识别微信聊天格式，解析去重后写入本地联系人档案 |
| 联系人一人一档案 | ✅ 已落地 | 每个联系人一个 JSON 档案，含画像 + 聊天记录 |
| 关系维护提醒 | ✅ 已落地 | 标记「特别关心」联系人，超过 N 天未联系自动生成问候建议并微信推送 |
| 语音转写顺序回填 | ✅ 已落地 | 复制含【语音】的聊天后，按顺序复制转写文字，自动回填语音内容 |
| 新联系人备注回填 | ✅ 已落地 | 发现新联系人时推送提醒，用户回复备注名后写入档案 |
| 本地 Web 看板 | ✅ 已落地 | 日志、档案、语音、意图、设置五个 Tab，含后台活动日志 |
| 桌面壳（Electron） | 🚧 已搭建 | 可打包用户端，核心能力已具备 |
| 聊天意图识别 / 信息抽取 | 🚧 已搭建基础版 | 新聊天归档后事件触发（带冷却），调用 intent Agent 识别任务/DDL/日程/事项/等待回复 |
| 主动提醒 / 日程事项引擎 | 🚧 已搭建基础版 | 消费 intents.json，纯检查 + 到期按需生成提醒文案，支持免打扰时段，每 15 分钟执行 |
| 群聊消息模型 | ⏳ 待设计 | 群聊识别、群聊档案、任务指向判断 |
| 智能回复建议 | ⏳ 待开发 | PC 端消息接管 + 语境化回复草稿 |
| 图片/视觉理解 | ⏸️ 暂缓 | 后续接视觉模型生成图片描述，当前不保存图片文件 |

---

## 2. 与既有战略的关系（延续而非重启）

V3 不是从零开始的新点子，而是小鹈鹕核心战略 **Proactive Relational Engineering / 关系操作系统（Relationship OS）** 的**个人场景落地**：

| 阶段 | 场景 | 用户 | 形态 |
|------|------|------|------|
| V2 / BP 阶段 | 医疗院后连续服务（骨科/齿科随访） | 医生（B）→ 患者（C） | 企业微信嵌入的连续服务智能体 |
| **V3（本项目）** | **个人社交聊天记录管理** | **个人（C 端）** | **PC 客户端 + 移动端 + 输入法** |
| 终局 | 通用智能终端的长期主动协作底座 | 泛在 | SDK / API 输出 |

核心内核不变：**跨会话长记忆、主动时机判断、关系状态机、反馈学习**。变的是场景从"医生维护患者关系"切换到"个人维护自己的社交关系与事务"。

> 一句话：医疗是验证沙盒，**个人社交记录是第一个规模化 C 端场景**。

---

## 3. 背景与用户痛点

### 3.1 三个核心痛点

1. **关系记忆断层**：人的社交关系分散在微信、QQ、飞书、邮件等多个平台，谁说过什么、上次聊到哪、对方的近况——全部靠人脑记。关系维护成本高，重要的人容易被冷落。

2. **任务从聊天中流失**：大量工作任务、承诺、约定散落在聊天记录里（"下周一把方案发我"、"周五前确认报价"）。没有人在意时，这些 DDL 就悄悄溜走，直到对方催或自己错过。

3. **回复负担与时机焦虑**：消息一多，回不过来；回什么、什么时候回、用什么语气——需要反复权衡。错过重要消息或回错时机的代价很高。

### 3.2 现有方案的缺口

| 现有工具 | 局限 |
|----------|------|
| 系统提醒/闹钟 | 只有时间，不懂语义，无法从聊天自动生成 |
| 通用 AI 助手 | 只在被问时才答，不知道你的聊天上下文，无法主动 |
| 传统任务管理（Todo 类） | 需要手动录入，与聊天脱节，不会自动提醒 |
| 输入法联想 | 只做词级联想，不理解关系与上下文 |

**空白点**：缺少一个"看懂你的全部聊天、记住所有关系、主动帮你管事、帮你回话"的持续在场助手。

---

## 4. 目标用户与核心场景

### 4.1 用户画像

- **高消息量职场人**：学生/创业者/销售/项目经理/自由职业者，每天大量社交消息，多线程任务并行。
- **重视关系但精力有限的人**：想维护好亲友、客户、合作伙伴关系，但时间不够。
- **有"怕错过"焦虑的人**：怕错过 DDL、怕忘回重要消息、怕冷落重要的人。

### 4.2 核心场景

1. **任务识别与 DDL 管理**：聊天里出现"下周发方案"→ 自动提取任务 + 截止时间 → 自动建 DDL → 到期前主动提醒 → 帮助推进完成。
2. **关系记录与分析**：每个联系人一个 Agent 画像，记录对方近况、偏好、承诺、情绪变化；重要关系动态一眼看清。
3. **主动提醒**：到点提醒工作任务；识别到需要回应的消息但用户还没回时，主动提醒"XX 还在等你的回复"。
4. **智能回复建议**：PC 端打开应用接管消息，基于历史聊天记录自动生成贴合语境与关系风格的回复草稿。
5. **移动端推送 + 输入法建议**：手机端主动推送重要消息摘要；用户打字时，输入法实时推送建议回复。

---

## 5. 核心理念与设计原则

1. **主动而非被动**：AI 在用户没有指令时依然在场，判断"何时该出现"。
2. **长记忆而非瞬时**：跨会话、跨平台、跨时间地维护每个关系与每件任务的完整状态。
3. **无感嵌入**：不改变用户的聊天习惯，AI 像一层智能层叠加在既有聊天工具之上。
4. **隐私优先**：聊天记录是最高敏感度数据，默认本地优先、用户授权、可随时删除。
5. **人机边界清晰**：AI 是"提醒者、建议者、辅助者"，不是"替身"——发送与决策的最终权归用户（与医疗 BP 的"AI 拟稿 + 医师审核"一脉相承）。
6. **人格化反馈**：用"小鹈鹕吃东西"等 IP 化动效替代冰冷进度，让"处理数据"变成有温度的体验。

---

## 6. 功能架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                      多端形态层                                │
│      PC 客户端（消息接管 + 回复建议）                            │
│      移动端（主动推送 + 消息摘要）                              │
│      输入法（打字时实时建议回复）                               │
└──────────────────────────────────────────────────────────────┘
                              ↓ ↑
┌──────────────────────────────────────────────────────────────┐
│                     核心功能层                                │
│  聊天记录同步 · 关系 Agent · 任务/DDL · 主动提醒 · 回复建议    │
└──────────────────────────────────────────────────────────────┘
                              ↓ ↑
┌──────────────────────────────────────────────────────────────┐
│                  Relationship OS 内核（复用）                  │
│   状态层 · 记忆层 · 策略层 · 触发层 · 行动层 · 反馈层          │
│   （心跳机制 · 分层记忆 · 时序状态机 · 剧本/意图解耦）          │
└──────────────────────────────────────────────────────────────┘
                              ↓ ↑
┌──────────────────────────────────────────────────────────────┐
│                  数据接入层（多平台适配器）                     │
│      微信 · QQ · 飞书 · 钉钉 · Telegram · 邮件 · 短信 ...      │
└──────────────────────────────────────────────────────────────┘
```

---

## 7. 核心功能详解

### 7.1 聊天记录自动捕获（剪贴板监听，MVP 已确认）

小鹈鹕常驻后台（托盘运行），**持续监听系统剪贴板**。当检测到剪贴板内容匹配"聊天记录格式"时，自动解析并存入本地聊天记录数据库——用户只需在微信里复制，无需额外粘贴操作。首次使用时签署同意须知，之后静默处理。合规、无需 hook、无封号风险。

**自动捕获流程**：
1. 监听剪贴板变化；
2. 格式嗅探：识别剪贴板内容是否命中「昵称 日期 时间 内容」的聊天记录模式（连续多条命中才判定为聊天记录，避免误触发）；
3. 命中 → 自动解析 → 去重 → 写入本地数据库；
4. 未命中 → 立即丢弃，不存储、不上传（隐私保护，见第 9 节）。

**解析规则**：微信复制出的原始文本为「昵称 + 时间 + 内容」的连续序列：

```
六壹 2026年08月15日 20:35 拿喜茶顺便逛街去了
Dorveille. 2026年08月15日 20:35 要排队啊
Dorveille. 2026年08月15日 20:36 [动画表情]
```

- 按「昵称 YYYY年MM月DD日 HH:mm 内容」切分每条消息；相邻消息以空白分隔。
- 结构化字段：{ 发送人, 时间戳, 消息类型, 内容 }。
- 占位符处理：`[动画表情]`、`[图片]`、`[语音]`、`[视频]`、`[文件]` 等标注为对应消息类型，不参与文本语义分析。
- 边界情况：昵称含空格/日期、跨天/跨群聊混贴——解析器需容错并允许用户修正。

- **统一时间线**：多次复制捕获的消息聚合为统一时间线，按时间戳去重合并，可按联系人/群聊检索。

#### 7.1.1 处理反馈：小鹈鹕"吃东西"动画（品牌 IP 小巧思）

识别到聊天记录格式、开始处理聊天数据的瞬间，小鹈鹕 logo 播放**"张嘴吃下"的进食动画**，用"吞下聊天记录 → 咀嚼消化 → 满足收尾"的隐喻，把冷冰冰的数据处理过程人格化。

- **时机**：格式嗅探命中 → 解析入库期间持续播放；处理完成时收尾（如满意点头 / 打个嗝 / 羽翼轻拍）。
- **价值**：① 给用户即时的"正在处理"反馈，替代冰冷的进度条；② 强化"小鹈鹕吃掉你的聊天，帮你消化成记忆"的品牌叙事；③ 与"长记忆"产品理念呼应——数据被"吃进去 → 消化 → 长成长期记忆"。
- **落地形态**：托盘图标动效 + 主窗口 logo 动效，后续可作为全局 Loading / 空状态 / 启动屏的统一视觉语言。

#### 7.1.2 语音消息：占位 + 顺序回填（已落地）

微信复制语音消息时只得到 `【语音】` 占位，无法直接拿到音频内容。当前采用**用户主动转写 + 顺序回填**：

1. 复制含 `【语音】` 的聊天记录 → 系统保留占位，进入 `voice-pending.json` 待回填队列；
2. 用户按顺序复制微信「转文字」后的纯文本；
3. 系统按队列顺序把第 1 条文本填给第 1 条语音、第 2 条填给第 2 条，依此类推；
4. Dashboard「语音」页可查看待回填列表，支持手动跳过。

> 合规性：全程由用户主动复制/转写/粘贴，不碰微信数据库、不自动读取聊天内容，可商业化。

#### 7.1.3 图片消息：当前暂缓，后续接视觉模型

微信复制图片消息时，剪贴板同时带有文字占位和真实图片数据。当前阶段**不开发图片存储与展示**，`[图片]` 仍按普通非文本消息处理。

后续接入视觉模型后，再考虑：
- 保存剪贴板图片或直接交给视觉模型；
- 生成图片描述并存入消息内容（如 `content="图片描述：..."`）；
- 需要处理多张图片时的顺序对应问题（可复用语音回填的队列思路）。

#### 7.1.4 群聊消息设计（待细化，建议方案）

群聊与私聊的关键差异是：**一条复制内容里会出现多个发送人**，不能简单归到某个联系人档案。

**建议的识别规则**：
- 一次复制中，非自己的发送人 ≥ 2 个 → 判定为群聊消息；
- 如果只有 1 个非自己发送人 → 按私聊处理（兼容“自己 + 对方”的双人对话）。

**建议的数据模型**：
```json
{
  "type": "group",
  "name": "项目群",
  "members": ["Hank", "杨勇", "六壹"],
  "messages": [
    { "name": "Hank", "ts": "2026-08-18 22:02", "type": "text", "content": "..." }
  ],
  "profile": {}
}
```

**待细化的问题**：
- 剪贴板复制群聊内容通常不包含群名；首次识别到新群聊时，可让用户手动命名，或先用 `group-<hash>` 占位。
- 群聊消息是否同时回写成员个人档案？建议：个人档案记录“在群聊中出现过”，但完整群聊上下文保留在群聊档案中。
- 群聊中的任务/DDL 需要判断指向：`@我 / @某人 / 对大家说 / 未指定`。指向不明确时进入“待确认”，不自动创建提醒。
- 关系维护提醒是否把“群聊互动”计入个人冷落？建议私聊和群聊分开统计，避免“只在群里说话”被误判为关系热络。
- 群聊消息去重、成员改名、同一人多个昵称等问题需要后续对齐。

### 7.2 关系 Agent（多社交关系记录与分析）

- **一人一档案**：每个联系人/群聊一个独立的关系画像，包含：关系类型（亲友/客户/同事）、近况、偏好、敏感话题、重要承诺、情绪趋势、上次互动时间。
- **关系健康度**：AI 计算"关系活跃度/亲密度"指标，标记正在变冷的关系，提示"该联系一下了"。
- **关系维护提醒（已落地）**：用户可对联系人标记「特别关心」，系统按 `relationCheck.days` 阈值检测冷落关系，超过 N 天未联系时自动生成一条自然问候建议，并通过微信推送给用户。
- **多 Agent 协作**：关系 Agent 负责"人"，任务 Agent 负责"事"，二者共享记忆、相互调用（与 Companion Agent 框架的多模块协作一致）。

### 7.3 聊天意图识别与信息抽取（与提醒解耦）

> 定位：**只负责“看懂聊天”，不负责“什么时候提醒”**。这是一个独立的语义理解层。
> 当前状态：基础版已搭建（`extract_intents.js` + `intent` Agent + Dashboard「意图」页），新聊天归档后事件触发，带冷却避免频繁调模型。

- **意图识别**：识别聊天中的承诺、请求、约定、日程、事项、待办、等待回复等语义类型。
- **实体抽取**：抽取出任务描述、截止时间、相关人、来源消息、置信度、状态等结构化字段。
  - 示例：聊天中"下周一把方案发我" → `{ 类型: 任务, 描述: "把方案发给对方", 截止: 下周一, 相关人: 对方, 来源: 该条消息 }`。
- **时间表达归一化**：把"下周一 / 周五前 / 后天 / 月底 / 这周末"等自然语言时间，结合消息时间戳归一化为具体时间；含糊表达（"尽快"）标记为"无明确截止"，仅作待办。
- **输出统一事件/事项模型**：任务、DDL、日程、提醒事项统一输出为结构化条目，供提醒引擎消费。
- **人机确认**：AI 抽取的结果先进入"待确认"队列，用户一键确认/修正/忽略，避免误判；确认与修正是对模型的隐式反馈。

### 7.4 主动提醒 / 日程与事项引擎（与抽取解耦）

> 定位：**只负责“什么时候提醒、怎么提醒”**，消费 7.3 抽取出的结构化条目，也支持用户手动创建日程/事项。
> 当前状态：基础版已搭建（`remind.js` + 微信推送 + OpenClaw cron 每 15 分钟执行），支持 DDL/日程/事项提醒节点、免打扰时段、按需生成提醒文案。

- **DDL 提醒**：任务截止前分时段提醒（如提前 1 天 / 当天 2 小时前）。
- **日程提醒**：支持会议、约会、生日、纪念日等时间点提醒。
- **事项提醒**：一次性提醒、重复提醒（每天/每周/每月）、位置提醒等。
- **沉默检测**：识别到"对方在等回复但用户未回"时主动提醒（"XX 昨天问你的方案，还没回复"）。
- **主动推进**：不仅提醒，还主动建议下一步动作（"要不要现在把方案初稿发给张总？"），甚至辅助起草。
- **提醒策略**：受用户可配置的免打扰时段约束，避免在深夜/会议时段打扰；高频提醒可被用户一键降噪。
- **免打扰时段（已落地）**：`doNotDisturb` 配置开始/结束时间，支持跨天；免打扰期间不推送，结束后自动补推。
- **按需生成文案（已落地）**：平时纯代码检查 0 token，只有真正到提醒节点才调用小模型生成提醒消息。
- **多通道触达**：微信推送 / PC 弹窗 / 移动端通知 / 后续输入法。

### 7.5 智能回复建议（PC 端）

- **消息接管**：PC 端打开应用后，实时接收并展示消息流。
- **语境化回复草稿**：基于该联系人的历史聊天记录、关系风格、当前语境，生成多条贴合语气的话术备选（正式/轻松/简洁）。
- **一键采用/微调**：用户确认或修改后发送，AI 持续学习用户的表达风格。

### 7.6 移动端推送 + 输入法建议

- **主动消息推送**：重要消息摘要、DDL 提醒、关系冷落提醒，主动推到手机通知栏。
- **输入法建议回复**：用户打字时，输入法实时给出整句建议回复（而非词级联想），基于当前会话上下文与关系画像。

### 7.7 功能拓展方向（roadmap 预留）

- 关系知识图谱、情感分析进阶
- 多模态（图片/语音消息理解）
- 群聊/会议纪要提炼、行动项抽取
- 日程与日历深度联动
- 多 Agent 协作与插件市场
- 面向团队/企业的协作版

---

## 8. 技术架构设想（复用既有资产）

V3 直接继承既有 Relationship OS 与 Companion Agent 框架的能力：

| 内核能力 | 在 V3 中的映射 |
|----------|----------------|
| 心跳机制（主动扫描） | 定时扫描任务 DDL、未回消息、关系冷落信号 |
| 分层记忆（短期/中期/长期） | 工作缓存 → 联系人关系画像 → 长期关系档案归档 |
| 时序状态机 | 任务状态机（待办/进行中/完成）+ 关系状态机（亲密度/阶段） |
| 策略层（时机判断） | 决定"何时提醒、何时建议回复、何时主动关心" |
| 触发层（多通道分发） | PC 弹窗 / 手机推送 / 输入法 / 日历 |
| 剧本/意图解耦 | 通用关系内核与具体场景（工作/亲友/客户）意图模板解耦 |
| 反馈学习 | 用户"采纳/修改/忽略"回复建议 = 隐式偏好标注，持续学习 |

### 关键架构新增点（相对 V2）

1. **多平台数据接入层**：新增微信/QQ/飞书/邮件等适配器（这是 V3 相比医疗场景最大的新增工程）。
2. **端侧能力**：PC 客户端、移动端推送、输入法 SDK 三个新端。
3. **隐私沙箱**：聊天数据的高敏感度要求更强的本地优先与端侧处理能力。

### 后续架构优化：常驻 Node 服务直连 Gateway

> 当前 `extract_intents.js` / `remind.js` 都是**每次重新拉起 `openclaw` CLI 子进程**去连接 Gateway，再完成 Agent 调用或微信发送。优点是实现简单、隔离好；缺点是每次都有进程启动 + CLI 初始化 + WebSocket 握手开销，频繁调用时更容易遇到 CLI 挂起/超时。

后续设计方向：

- 增加一个常驻 Node 服务（如 `gateway-client.js`），启动时建立并保持到 Gateway 的 WebSocket 长连接。
- 所有 Agent 调用、消息发送统一走这个常驻连接，不再每次 spawn `openclaw` CLI。
- 需要封装：
  - Gateway WebSocket 连接与鉴权
  - Agent turn 请求 / 响应协议
  - `sessions_spawn` 子 Agent 结果获取
  - 微信消息发送协议
  - 断线重连与超时控制
- 收益：更低调用开销、更快响应、更稳定，适合正式产品化后的高频调用。

---

## 9. 数据与隐私（V3 的头号约束）

- **默认本地优先**：聊天记录默认存本地/私有节点，推理尽量端侧或私有化部署，数据不出域。
- **剪贴板隐私**：只监听并识别"聊天记录格式"，非聊天内容（密码/验证码/普通文本）立即丢弃、不落盘、不上传。
- **用户完全授权**：接入哪些平台、哪些联系人的记录、是否允许上传——全部用户显式控制。
- **可遗忘**：随时可删除某段记录或某个关系画像。
- **合规红线**：遵守《个人信息保护法》；微信等封闭生态的数据接入必须在合规前提下进行（见第 11 节风险）。

---

## 10. 竞品与差异化

| 竞品/方向 | 我们的差异化 |
|-----------|--------------|
| 通用 AI 助手（ChatGPT/Claude/豆包等） | 它们不知道你的聊天，我们是"长在聊天记录上的 AI" |
| 传统任务管理（Todoist/滴答清单） | 它们要手动录入，我们从聊天自动提取并主动推进 |
| 输入法（搜狗/讯飞） | 它们做词级联想，我们做语境级整句回复建议 |
| 微信机器人/插件 | 多封闭式、单点功能；我们是跨平台的统一关系内核 |

**核心护城河**：不是"更聪明的模型"，而是**沉淀在用户侧的长期关系时序数据**——谁、何时、说了什么、关系如何演变，这条数据飞轮一旦转动，后来者无法跳过时间门槛复制。

---

## 11. 关键风险与待决策问题（已收敛 + 待决策）

> 已确认的决策不再开放；以下仅保留尚未关闭的问题。

1. **平台接入（已确认：剪贴板监听自动捕获）**：小鹈鹕常驻后台监听剪贴板，识别到聊天记录格式即自动解析入库。用户只需在微信复制，签署同意须知后静默处理。合规、无需 hook、无封号风险。
   - **遗留风险**：解析容错（昵称含空格、占位符、跨天混贴）；非实时（依赖用户复制，非后台自动抓取）；实时接入微信仍无官方通道，作为后续专项课题。
   - **语音（已决策）**：采用「用户转文字 + 顺序回填」，不抓微信本地语音文件，避免解包数据库。
   - **图片（已决策）**：当前暂不开发图片存储/展示，后续接视觉模型生成图片描述。

2. **隐私边界与商业模式**：C 端个人聊天数据极敏感，当前路径为「本地优先 + 用户主动复制/授权」。商业化时必须走微信官方能力（公众号/小程序/企业微信）或用户主动上传，不能碰本地数据库/逆向。

3. **MVP 范围（已收敛）**：当前已落地「剪贴板捕获 + 联系人档案 + 关系维护提醒 + 语音回填 + 本地看板」。下一步核心是「任务/DDL 提取 + 主动提醒」。

4. **单机 vs 云端**：当前先做本地 PC 端 + 微信推送；移动端推送和输入法建议放到 V1.1/V1.2。

5. **群聊消息模型（待设计）**：涉及多人时不能简单归入单人档案。需明确群聊识别、群聊命名、成员归属、群聊任务指向、群聊互动是否计入个人冷落等规则。建议方案见 7.1.4，尚未最终确认。

---

## 12. 建议的 MVP 与路线图（草案）

### 当前已落地（2026-08）
- ✅ 聊天记录自动捕获：剪贴板监听 → 识别聊天记录格式 → 解析去重 → 联系人档案
- ✅ 联系人一人一档案 + 画像
- ✅ 关系维护提醒（特别关心 + 冷落检测 + 微信推送）
- ✅ 语音转写顺序回填
- ✅ 新联系人备注回填
- ✅ 本地 Web 看板（聊天 / 档案 / 语音 / 意图 / 设置）
- ✅ 意图识别基础版：新聊天归档后事件触发（带冷却），调用 intent Agent 识别任务/DDL/日程/事项/等待回复
- ✅ 主动提醒基础版：每 15 分钟纯检查 intents.json，到期按需生成提醒文案，支持免打扰时段
- ✅ OpenClaw cron 每日执行关系维护检查 + 每 15 分钟执行提醒（意图识别改为事件触发，不再每小时 cron）
- ✅ Electron 桌面壳搭建

### MVP 下一阶段（待开发）
- 意图识别效果调优：确认/忽略反馈回流，优化 intent Agent 输出
- 微信回复确认：用户在微信里回复“确认/忽略”直接更新意图状态
- 群聊消息模型设计：群聊识别、群聊档案、任务指向判断
- 统一时间线 + 检索（支撑上述能力的基础视图）

### V1.1
- 关系健康度可视化（关系活跃度/亲密度）
- 回复建议（PC 端）
- 移动端推送

### V1.2
- 图片/多模态理解（视觉模型生成图片描述）
- 输入法建议回复
- 多平台接入扩展（开放 API 平台）
- 关系知识图谱 / 情感分析进阶

---

*本文档持续更新；第 6/7/11 节已按当前实现状态细化，后续随开发进展同步。*
