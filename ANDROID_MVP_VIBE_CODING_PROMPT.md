# 小鹈鹕 Android MVP 开发需求 Prompt（以 ai-calendar 为基线）

> Android 修订说明：Android 端不使用 DSH、Harness、Agent.md、Skill Catalog 或 PC 工作区。Android App 直接通过 HTTPS 调用云端 LLM，由 `ai-calendar` 的白名单工具调用层完成日历数据操作。本文中原有的 DSH 相关内容只适用于 PC 端，不属于 Android MVP 交付范围。

Android 开发基线为 `<工作区>/ai-calendar`（本机为 `C:\Users\<用户名>\ai-calendar`）。开发前先阅读它的 `README.md`、`docs/开发方案.md`、`core/llm/`、`core/records/`、`core/api/routes/` 和现有测试，优先复用现有实现。

## 0. 使用方式

你是一名负责交付 Android MVP 的全栈 Agent。请先阅读本文件，再检查 `ai-calendar` 现有实现，优先复用已有的数据模型、API、云端 LLM 工具调用和业务规则。不要重写已经可用的核心逻辑，不要为了实现功能而扫描或修改无关目录。

每完成一个阶段，都必须：

1. 说明修改了哪些文件以及原因；
2. 运行对应测试或给出无法运行的具体原因；
3. 用真实用户流程验收；
4. 保持已有 WebUI、微信通道和桌面端功能不回归。

本 MVP 面向 Android 手机，核心目标是让用户在手机上完成“查看和接收事项提醒”，并通过系统输入法完成“复制聊天记录、自动归档、获得回复建议、直接询问 AI、创建事项”。

## 1. 产品边界

### 1.1 MVP 必须包含

- Android App 主界面；
- 日历/事项页，统一展示待办、提醒和日程；
- 到期提醒通知；
- Android 输入法扩展（IME）；
- 剪贴板固定格式聊天记录识别；
- 聊天记录自动归档；
- 联系人档案创建或更新；
- 根据联系人画像生成回复建议；
- 输入法内 AI 对话输入框；
- 通过一句话让云端 LLM 创建、修改或查询待办、提醒和日程；
- 多个用户昵称配置，用于识别不同微信号导出的聊天记录；
- 已完成事项自动归档，不再占用默认列表。

### 1.2 MVP 暂不包含

- 微信官方客户端深度嵌入；
- 自动发送微信消息；
- 云端同步多个设备；
- 复杂项目管理、子任务、标签体系；
- 输入法直接读取任意 App 的全部屏幕内容；
- 未经用户触发的后台全量聊天扫描。

## 2. 总体架构

```text
Android App
├── 事项/日历 UI
├── 本地缓存与通知调度
└── 设置/账号/连接状态

Android IME
├── 剪贴板格式检测
├── 聊天记录确认与归档
├── 回复建议卡片
└── AI 输入框

小鹈鹕 Core API
├── contacts：联系人和画像
├── conversations：聊天归档
├── tasks：待办和提醒
├── schedules：日程
├── intents：待确认意图
└── LLM Gateway：调用云端模型和白名单工具

Cloud LLM
├── 理解自然语言
├── 选择白名单工具
└── 通过 Core API 操作数据
```

Android 端不启动 DSH，也不依赖 PC 工作区。云端 LLM 负责自然语言理解和工具选择；Core API 负责参数校验、去重、权限和持久化。Android 客户端不得通过关键词自行判断用户意图，也不得直接编辑数据库或 JSON 文件。 

查询事项必须走白名单查询工具，禁止把本地数据库、项目源码或文件路径发送给云端模型。

## 3. 统一数据模型

### 3.1 待办

```json
{
  "id": "todo_xxx",
  "type": "todo",
  "title": "整理实验数据",
  "detail": "可选背景",
  "status": "open|in_progress|done|archived",
  "dueAt": null,
  "source": "manual|agent|chat_import",
  "createdAt": "ISO-8601",
  "completedAt": null,
  "archivedAt": null
}
```

待办表示用户需要做的事情，不要求一定安排到某个时间。没有时间的事项仍然可以存在。

### 3.2 提醒

```json
{
  "id": "reminder_xxx",
  "type": "reminder",
  "title": "下班打卡签退",
  "detail": "可选内容",
  "status": "open|done|archived",
  "kind": "once|cron",
  "dueAt": "ISO-8601 或 null",
  "cron": "0 16 * * 1-5",
  "nextAt": "ISO-8601",
  "createdAt": "ISO-8601",
  "completedAt": null,
  "archivedAt": null
}
```

提醒表示在某个时间点通知用户。周期提醒由系统调度器执行，不调用模型。

### 3.3 日程

```json
{
  "id": "schedule_xxx",
  "type": "schedule",
  "title": "儿科学缓考",
  "detail": "可选内容",
  "startAt": "2026-09-08T00:00:00+08:00",
  "endAt": null,
  "allDay": true,
  "status": "open|done|archived",
  "createdAt": "ISO-8601",
  "completedAt": null,
  "archivedAt": null
}
```

日程表示一段活动或全天安排。没有具体时分但有明确日期的考试、招聘、旅行等，允许创建全天日程。

### 3.4 联系人与多个用户昵称

设置数据必须支持：

```json
{
  "userProfiles": [
    { "id": "self_1", "nickname": "小明", "enabled": true },
    { "id": "self_2", "nickname": "明明", "enabled": true }
  ]
}
```

要求：

- 昵称不能为空，去除首尾空格；
- 不允许重复昵称，忽略大小写和全角半角空格差异；
- 至少允许新增、编辑、删除、启用/停用；
- 至少保留一个启用昵称；
- 旧版单昵称配置自动迁移为一个数组元素；
- 剪贴板解析时，消息发送方命中任一启用昵称都归为用户本人；
- 联系人画像不能把多个用户昵称误识别成联系人；
- UI 明确显示“这些昵称用于识别不同微信号中的自己”。

## 4. 云端 LLM 与工具调用约定

Android 端直接使用 `ai-calendar/core/llm/` 的模型客户端和工具调用层。不要创建 DSH 会话、Harness 启动器、PC 工作区或 `Agent.md` 注入流程。系统提示词只描述产品角色和工具使用规则，工具定义由 Core API 提供。

至少提供以下 Skill：

- `records_query`：查询待办、提醒、日程、待确认意图；
- `todo_create/update/complete`：创建、修改、完成待办；
- `reminder_create/update/complete`：创建一次性/周期提醒、修改和完成提醒；
- `schedule_create/update/complete`：创建、修改、完成日程；
- `chat_import`：接收已确认的剪贴板聊天记录并归档；
- `reply_suggestion`：基于联系人画像生成回复草稿；
- `intent_propose`：从聊天记录提出待确认意图；
- `llm_answer`：处理通用问答，不获得文件或命令能力。

每个工具必须写清楚用途、输入参数、输出结构、必填字段、失败原因、去重规则和示例。工具的实际读写必须在 Core 服务端完成，不能让模型或 Android 客户端直接改数据库。

自然语言流程要求：

- 用户直接说“安排上”“记到日历”“提醒我”“记一个待办”，由云端 LLM 自主选择对应工具；
- 用户询问“我现在有什么待办/提醒/日程”，由云端 LLM 调用查询工具；
- 一句话包含多个独立日期时，模型可以创建多条记录；
- 从导入聊天记录中推断出的事项只能进入待确认意图；
- Android MVP 不提供文件、命令或 DSH 执行工具；复杂任务暂时只返回能力边界或交给后续版本；
- 信息不足时先询问用户，不能猜测，也不能回退为扫描项目；
- 工具返回错误时，模型必须向用户报告真实错误，不能回复“已创建”。

## 5. Android App 具体开发任务

### 阶段 A：工程骨架与 API 客户端

任务：

1. 创建 Android App 模块和 IME Service 模块；
2. 选择稳定、易维护的 UI 技术并统一主题；
3. 实现 Core API 地址、连接状态和认证配置；
4. 实现网络超时、重试、离线提示和错误展示；
5. 所有 API 响应统一映射为客户端模型；
6. 不在客户端保存 DeepSeek API Key；
7. 支持开发环境地址和局域网设备地址配置。

验收：

- App 可安装并启动；
- API 不可用时显示可理解的离线状态，不崩溃；
- 请求超时不无限转圈，最长等待时间可配置且有失败提示；
- Android Studio 单元测试和静态检查通过；
- 不影响现有 Node 测试。

### 阶段 B：日历与事项页

任务：

1. 顶部显示月份切换和返回今天；
2. 日期格显示当天有多少待办、提醒和日程；
3. 点击某天显示该日具体日程和提醒；
4. 旁边或下方提供“全部待办”面板，默认不按“今日待办”拆分；
5. 待办、提醒、日程使用明显不同的颜色和图标；
6. 提供统一“新建事项”入口，可选择待办、提醒、日程；
7. 支持查看、编辑、完成、删除；
8. 完成事项立即从默认列表移入历史/归档区域；
9. 列表分页或滚动加载，避免一次渲染全部历史数据；
10. 空状态、加载状态、错误状态完整。

验收：

- 创建一个待办后，它出现在全部待办中，不强行出现在某一天；
- 创建一次性提醒后，只在对应时间显示；
- 创建周期提醒后，日历显示规则和下一次触发时间；
- 创建全天日程后，点击日期可以看到；
- 完成事项后默认列表不再显示，归档页可以找到；
- 切换日期只改变日期详情，不改变全部待办列表；
- 重启 App 后数据仍然存在；
- 竖屏小屏设备不发生内容截断。

### 阶段 C：本地通知与自动归档

任务：

1. 请求 Android 通知权限并解释用途；
2. 将一次性提醒和周期提醒转换为本地通知调度；
3. 支持通知点击进入事项详情；
4. 周期提醒触发后计算下一次时间；
5. 一次性提醒完成后自动归档；
6. 日程结束或用户标记完成后自动归档；
7. 任务/提醒/日程状态变更后同步本地调度；
8. App 重启、手机重启后恢复调度；
9. 通知重复防护，不能同一事项重复弹出；
10. Android 电池优化限制下显示调度状态。

验收：

- 测试用短时间提醒可以准时通知；
- 周期提醒每个周期只通知一次；
- 已完成事项不会继续通知；
- 已归档事项不出现在默认首页和待办列表；
- 通知点击能打开正确事项；
- 拒绝通知权限时，App 明确提示用户如何开启。

### 阶段 D：Android 输入法与剪贴板归档

任务：

1. 实现最小可用输入法服务，支持普通文字输入、删除、空格、回车和候选区；
2. 在输入法候选区下方增加可展开的 AI 区域；
3. 检测剪贴板变化时只读取文本，不读取图片和无关内容；
4. 仅匹配固定聊天记录格式，例如：

   ```text
   昵称 2026-09-13 14:20
   消息内容
   ```

5. 对格式不完整的文本不自动导入，提供“识别失败”原因；
6. 识别成功后展示联系人、消息条数和预览；
7. 首次导入必须让用户确认；
8. 确认后调用 `chat-import` API 自动保存联系人和聊天记录；
9. 根据联系人画像调用回复建议接口；
10. 生成 3 条以内的回复草稿；
11. 点击回复草稿只回填输入框，不自动发送；
12. 同一剪贴板内容不得重复归档；
13. 用户切换多个微信号后，按昵称数组识别本人消息。

验收：

- 复制合法聊天记录后，输入法出现导入卡片；
- 用户取消后，不写入任何联系人或聊天数据；
- 用户确认后，联系人和消息记录可在 App 中查询；
- 重复粘贴完全相同内容不会产生重复消息；
- 包含两个用户昵称时，两个昵称都识别为“我”；
- 普通网页文本、单句聊天文本不会误触发导入；
- 回复建议能回填当前微信输入框；
- 输入法关闭再打开后不丢失正在编辑的文字。

### 阶段 E：输入法内 AI 交互

任务：

1. AI 区域包含输入框、发送按钮、收起按钮和结果区；
2. 支持通用问答；
3. 支持一句话查询事项；
4. 支持一句话创建/修改待办、提醒和日程；
5. 支持上下文引用当前剪贴板聊天和联系人画像，但必须让用户知道引用了什么；
6. Agent 请求执行时显示处理中、成功、失败三种状态；
7. 创建操作返回真实记录标题、类型、时间和 ID；
8. 复杂 AI 工作显示执行进度并允许离开输入法后在通知中查看结果；
9. 用户确认类操作必须有明确按钮；
10. 不自动发送消息，不自动确认高影响操作。

验收示例：

- 输入“我周一下午三点开会，记到日历”，创建一条日程；
- 输入“明天 10 点提醒我给导师发消息”，创建一次提醒；
- 输入“记一个待办：买充电器”，创建无时间待办；
- 输入“现在有哪些提醒”，只返回提醒，不混入待办和日程；
- 输入“帮我整理这个文件”，在 Android MVP 中明确提示暂不支持文件操作，不伪造执行结果；
- 输入不完整的“下周安排一下”，Agent 询问缺少的事项和时间；
- 网络断开时在输入法内显示失败原因，不出现永久加载。

### 阶段 F：多昵称设置

任务：

1. 设置页增加“我的微信昵称”管理入口；
2. 支持新增、编辑、删除、启用和停用；
3. 以标签/列表形式展示多个昵称；
4. 保存前做重复、空值和至少一个启用项校验；
5. API 兼容旧版单昵称字段；
6. 导入预览页显示命中的用户昵称；
7. 允许用户手动修正本次导入中的本人昵称；
8. 修改昵称后不重写历史消息，只影响后续识别。

验收：

- 添加“微信号 A”和“微信号 B”后，两者都能识别为本人；
- 删除一个昵称后，历史聊天不被删除；
- 停用全部昵称时保存失败；
- 重复昵称被拒绝并说明原因；
- 老用户升级后原昵称仍然有效。

## 6. Core API 任务

在现有 Node Core 中补齐移动端需要的 API，保持 WebUI API 向后兼容。

建议接口：

```text
GET  /api/mobile/bootstrap
GET  /api/settings/user-profiles
PUT  /api/settings/user-profiles
GET  /api/records?type=todo|reminder|schedule&status=open
GET  /api/records/calendar?from=...&to=...
POST /api/todos
PATCH /api/todos/:id
POST /api/reminders
PATCH /api/reminders/:id
POST /api/schedules
PATCH /api/schedules/:id
POST /api/records/:id/complete
GET  /api/records/archive?page=1&pageSize=20
POST /api/chat-import/preview
POST /api/chat-import/confirm
POST /api/reply-suggestions
POST /api/agent/message
GET  /api/agent/tasks/:id
```

每个写接口必须：

- 校验类型和必填字段；
- 对时间统一使用 ISO-8601 和明确时区；
- 支持幂等键或来源消息 ID；
- 返回规范化后的完整记录；
- 错误使用稳定的错误码和中文说明；
- 不暴露 API Key；
- 不允许客户端直接传入任意文件路径执行命令。

重点修复：任何模型返回“已创建”前，都必须收到真实 API 成功结果。API 写入失败时，模型不得声称成功。

## 7. 自动归档规则

默认首页只展示未归档且未完成的事项。

- 一次性提醒触发并被用户确认完成后归档；
- 周期提醒永不因单次触发而归档，只更新 `nextAt`；
- 日程结束后可自动标记完成并归档；
- 待办由用户完成后归档；
- AI 任务完成后，关联待办进入完成态并归档；
- 归档记录保留可搜索；
- 归档失败不能删除原记录；
- 默认查询 API 不返回 `status=done|archived`，除非显式请求历史。

## 8. 安全与隐私

- 剪贴板内容只在识别到固定格式后读取和预览；
- 未确认的剪贴板内容不上传、不落库；
- 回复建议只写入输入框，不自动发送；
- API Key 只保存在 Core 服务端配置；
- Android 日志不得打印聊天原文、联系人画像或密钥；
- 网络请求使用 HTTPS；开发环境允许局域网 HTTP，但必须有明显配置项；
- 用户可以清空单个联系人、单段聊天和全部本地缓存；
- 删除操作必须二次确认；
- App 权限说明与实际权限一致。

## 9. 端到端验收清单

### 事项链路

- 创建无时间待办 → 首页显示 → 完成 → 默认列表消失 → 历史可查；
- 创建一次提醒 → 到点通知 → 点击通知进入详情 → 完成后归档；
- 创建周期提醒 → 到点通知 → 下一次时间正确 → 不启动 DSH；
- 创建全天日程 → 日期格有标记 → 点击日期显示 → 结束后归档；
- 查询待办/提醒/日程 → 结果类型严格隔离；
- 修改事项 → 列表、日历、通知全部同步。

### 剪贴板与输入法链路

- 复制合法聊天记录 → 自动识别 → 预览 → 用户确认 → 联系人和聊天归档；
- 同一内容重复复制 → 不重复导入；
- 多微信昵称 → 正确识别本人消息；
- 联系人画像 → 生成不超过 3 条回复建议；
- 点击建议 → 回填输入框，不自动发送；
- 输入法 AI 输入 → 通用问答可用；
- 输入法 AI 输入 → 创建/查询事项可用；
- 不支持的复杂任务 → 明确提示当前 Android MVP 能力边界，不伪造任务状态。

### 稳定性

- Core 不可用时 App 和输入法可恢复；
- 云端 LLM 超时后请求在有限时间内失败，不永久卡住；
- App 重启后事项和通知状态一致；
- Android 重启后提醒恢复；
- 小屏、深色模式、中文长标题下布局不溢出；
- 现有 WebUI、微信归档、Node 测试全部通过。

## 10. 交付顺序

按以下顺序实施，不要同时铺开所有页面：

1. Core API 统一模型、归档状态、幂等写入；
2. Android App 连接和事项列表；
3. Android 本地通知；
4. 多昵称设置和旧数据迁移；
5. 输入法基础输入与剪贴板预览；
6. 聊天归档和联系人画像；
7. 回复建议；
8. 输入法 AI 交互和云端 LLM 工具调用；
9. 统一系统提示词、工具定义和模型错误处理；
10. 端到端测试、打包和真机验收。

每个阶段都必须先完成可运行的最小闭环，再进入下一阶段。代码生成 Agent 不得只创建 UI 假数据；凡是页面中显示“已创建”“已保存”“已完成”，都必须来自后端真实成功响应。

## 11. 最终交付物

- 可安装的 Android debug APK；
- 可安装的输入法 APK 或同一 APK 内的 IME Service；
- Core API 启动与配置说明；
- 云端 LLM 系统提示词、工具定义和 Android 接口文档；
- 数据迁移说明；
- 真机验收记录；
- 自动化测试结果；
- 已知限制和下一阶段建议。
