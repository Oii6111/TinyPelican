# 小鹈鹕 V3 — 社交关系归档规则

> 这是「剪贴板捕获 → 一人一档案」管道的 Agent 侧行为契约。
> 常规归档由 `_process_batches.js` 自动完成；需要人工/Agent 介入时，严格按本文件执行。

## 目标

把剪贴板捕获的微信聊天记录，归档为「一人一档案」：每个联系人一个 JSON 档案，
包含（1）该联系人的**画像** `profile`，（2）与该联系人的**聊天记录** `messages`。
画像可由用户随时手工补充。

## 数据目录（均在 `C:\Users\Lenovo\v3\` 下）

| 路径 | 含义 | 你的权限 |
|------|------|----------|
| `batches/*.jsonl` | 待处理的聊天批次（每行一条消息 JSON） | 只读，处理完删除 |
| `contacts/<昵称>.json` | 每个联系人一个档案 | 读写（核心产物） |
| `inbox.jsonl` | 全量聊天流水 | 只读，不要改 |

> 说明：批次由 `_process_batches.js` 成功写入联系人档案后删除，不再移动到 `processed/`。

## 消息字段

每行 JSON：`{ "name": 昵称, "ts": "YYYY-MM-DD HH:mm", "type": "text|image|voice|video|file|动画表情|...", "content": "文本内容", "contact": "联系人昵称" }`

- `type` 为 `text` 时 `content` 是文本；其余类型（image/voice/video/file/动画表情…）`content` 为空字符串，代表一条非文本消息。
- `contact` 由监听器预先算好：非空时，本消息（无论谁发的）都归入 `contacts/<contact>.json`；为空时，退回按 `name` 分组。
- 文本里的 `[捂脸]`、`[OK]` 之类是微信表情字面量，按普通文本保留，不要解读。

## 联系人档案结构

`contacts/<昵称>.json` 结构示例：

```json
{
  "name": "联系人昵称",
  "remark": "",
  "updatedAt": "2026-08-20T02:14:11.000Z",
  "important": false,
  "profile": {
    "关系类型": "未知",
    "近况": "",
    "偏好": "",
    "重要承诺/待办": "",
    "敏感话题/注意点": "",
    "情绪趋势": "",
    "最近互动时间": ""
  },
  "messages": [
    { "name": "对方", "ts": "2026-08-16 20:27", "type": "text", "content": "你好" }
  ]
}
```

- `profile` 里的条目只写**有依据**的推断，拿不准写「待观察」，绝不臆测、绝不编造。
- **绝不覆盖或删除用户手写的内容**。若某条目已有用户手写内容，保留它，可另起 `（补充：…）` 追加分析。
- `messages` 按时间正序追加，并**去重**：若档案里已存在相同 `name + ts + type + content` 的记录则跳过。

## 处理流程

1. 列出 `batches/` 下所有 `*.jsonl`，按文件名时间序处理。
2. 逐行解析 JSON。按 `contact` 字段分组（监听器已按 `selfNicknames` 算好联系人归属）。
3. 确定每条消息归属的联系人：
   - `contact` 非空 → 归入 `contacts/<contact>.json`（完整对话：含对方和自己双方的消息）。
   - `contact` 为空 → 退回按 `name` 分组（每个昵称各建一档，只写该昵称自己的消息），并在回报里提醒用户补充 `selfNicknames`。
4. 对每个联系人：读取已有 JSON，把新消息按时间正序去重追加，增量更新 `profile` 和 `updatedAt`。
5. 成功写入后删除该批次 `.jsonl`。
6. 最后回报一行总结，例如：`✅ 本次归档 2 个联系人、新增 20 条消息。`

## 红线

- 所有数据只写本地 `C:\Users\Lenovo\v3\`，绝不上传、绝不通过任何渠道外发聊天内容。
- 只处理 `batches/` 里的文件，不要主动去读 `inbox.jsonl` 之外的聊天数据。
- 遇批次文件损坏（某行不是合法 JSON）→ 跳过该行继续，不要整批失败。
