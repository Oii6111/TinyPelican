# 小鹈鹕 AI V3（TinyPelican）

> **项目简介**：一个“长记忆 + 主动关系”的个人 AI 助手。用户只需要像平常一样复制微信聊天记录，小鹈鹕就会自动归档联系人、分析每段关系、提取任务与 DDL，并主动提醒、维护关系，成为时刻在用户身边的第二大脑。

---

## 愿景

> 让 AI 从“你叫它才动”的被动劳动力，变成“主动替你想到、提醒你、甚至帮你开口”的关系管家。

小鹈鹕的核心战略是 **Proactive Relational Engineering / 关系操作系统（Relationship OS）**：跨会话、跨平台、跨时间地维护每个关系与每件任务的完整状态。V3 是这个战略在个人社交场景的落地——先服务“个人”，再逐步扩展到多平台、多端。

### 核心价值主张

| 优势 | 一句话 |
|------|--------|
| 交互成本趋近于零 | “复制”即接入，不改变微信使用习惯 |
| 主动性反转 | 从“人分配工作给 AI”到“AI 替人想事” |
| 长记忆关系内核 | 不是工具，是“记得你的人” |
| 无感嵌入 | 智能叠加在聊天工具之上，不打扰现有习惯 |
| 隐私优先 | 本地优先，剪贴板只认聊天格式，非聊天内容立即丢弃 |

---

## 为什么做

1. **关系记忆断层**：社交关系分散在微信、QQ、飞书等平台，谁说过什么、上次聊到哪、对方近况，全靠人脑记，重要的人容易被冷落。
2. **任务从聊天中流失**：大量任务、承诺、约定散落在聊天记录里（“下周一把方案发我”“周五前确认报价”），没有人在意时 DDL 就悄悄溜走。
3. **回复负担与时机焦虑**：消息一多，回不过来；回什么、什么时候回、用什么语气，都需要反复权衡。

现有方案（系统提醒、通用 AI、Todo 类工具、输入法）都只解决片段，缺少一个“看懂聊天、记住关系、主动管事、帮你回话”的持续在场助手。

---

## 目标用户与核心场景

### 目标用户
- 高消息量职场人：学生、创业者、销售、项目经理、自由职业者
- 重视关系但精力有限的人：想维护亲友、客户、合作伙伴关系但时间不够
- 有“怕错过”焦虑的人：怕错过 DDL、怕忘回重要消息、怕冷落重要的人

### 核心场景
- 聊天里出现“下周发方案” → 自动提取任务 + DDL → 到期前主动提醒
- 每个联系人一个 Agent 画像：近况、偏好、承诺、情绪趋势
- 标记「特别关心」→ 超过 N 天未联系自动生成问候建议
- 识别到“对方还在等回复” → 主动提醒
- 后续：PC 端回复建议、移动端推送、输入法整句建议

---

## 当前能力（2026-08）

| 能力 | 状态 | 说明 |
|------|------|------|
| 微信 iLink 双向通道 | ✅ 已落地 | 扫码登录 + 长轮询收发消息，提醒/建议可直接推送到微信 |
| 剪贴板自动捕获聊天记录 | ✅ 已落地（默认关闭） | 在「记忆 → 记忆输入」开启；识别微信聊天格式，解析去重后写入本地档案 |
| 联系人一人一档案 | ✅ 已落地 | 每个联系人一个 JSON 档案，含画像 + 聊天记录 |
| 关系维护提醒 | ✅ 已落地 | 标记「特别关心」，超过 N 天未联系自动生成问候建议并推送 |
| 语音转写顺序回填 | ✅ 已落地 | 位于「记忆 → 记忆输入 → 剪贴板捕获」，按顺序回填语音内容 |
| 意图识别 / 待确认行动 | ✅ 已落地 | 新消息归档后自动识别，待确认行动页可 👍 确认 / 👎 忽略 |
| 主动提醒引擎 | ✅ 已落地 | 进程内定时调度（替代外部 cron），心跳间隔可配置 |
| 本地 Web 看板 | ✅ 已重构 | 顶部状态栏（心跳/主动级别/未读）+ 对话板块 + 四大板块子页导航 |
| DSH Agent 对话回复 | ✅ 已落地 | 所有通道对话默认走 DSH harness 大模型回复；WebUI 流式展示思考、工具调用、执行过程，最终回答独立显示 |
| Agent 事件裁剪与增量展示 | ✅ 已落地 | 工具结果限长、连续增量合并、任务接口 `afterSeq` 增量拉取，页面不因超长输出卡顿 |
| 联系人档案编辑/删除 | ✅ 已落地 | 联系人页可直接编辑备注与画像；支持清空聊天记录、删除联系人并同步清理 `inbox.jsonl` |
| 全局消息检索 | ✅ 已落地 | 聊天记录查看器可不选联系人直接按关键词搜索全部档案 |
| 桌面壳（Electron） | ✅ 已搭建 | 可打包用户端，窗口可自适应缩放 |
| 群聊消息模型 | ⏳ 待设计 | 群聊识别、群聊档案、任务指向判断 |
| 智能回复建议 | ⏳ 待开发 | PC 端消息接管 + 语境化回复草稿 |
| 图片/视觉理解 | ⏸️ 暂缓 | 后续接视觉模型生成图片描述 |

---

## 技术架构

V3 已按「五模块框架」重构，完整设计见 [ARCHITECTURE.md](ARCHITECTURE.md)。

```
输入通道(channels)          输出通道(channels)
微信 iLink / 剪贴板            微信 iLink 推送
      │ 消息                          ▲ 提醒
      ▼                               │
 记忆输入(ingest)           主动提醒(remind)
 解析→去重→归档              心跳 + 定时任务
      │                               │
      ▼                               │
  ┌─ 记忆框架(memory) ────────────────┘
  │  contacts / intents / relations
  │         ▲
  │         │ 读写
  └─ Agent 引擎(engine) ── 直连大模型 API，多 Provider
```

### 当前技术栈
- **引擎层**（`core/engine/`）：直连 OpenAI 兼容 API，用户可配置 Provider（SiliconFlow / OpenAI / DeepSeek / Ollama / 自定义）
- **Agent 层**（`core/agent/` + `agent/dsh-event-stream/`）：基于 DSH（DeepSeek Harness）headless 执行对话任务，自定义插件把 session/event 输出为 JSONL 事件流，WebUI 实时展示思考/工具调用/执行过程
- **通道层**（`core/channels/`）：微信 iLink 协议双向通道（扫码登录 + 长轮询），预留多平台扩展
- **记忆层**（`core/memory/`）：联系人档案、意图库、关系状态、看板对话、未读计数
- **输入管道**（`core/ingest/` + `core/capture/`）：Node 剪贴板监听与实时消息统一归档（唯一保留的 PowerShell 组件是 15 行的剪贴板传感器 `core/capture/clipboard-sensor.ps1`，仅负责读剪贴板）
- **主动层**（`core/remind/` + `core/status.js`）：进程内定时调度 + 心跳/主动级别/未读状态
- **本地看板**：Node.js HTTP 服务 + 原生 ES 模块前端（`dashboard/`，无构建步骤）
- **桌面窗口**：Electron（`app/`，启动时拉起核心服务并热重启）
- **数据存储**：本地 JSON / JSONL / TOML，默认不上传

### PowerShell 组件（仅两处）

- `start.ps1`：开发模式一键启动——后台拉起核心守护进程，并打开 Edge 应用窗口
- `core/capture/clipboard-sensor.ps1`：剪贴板传感器（约 15 行），由 Node 核心作为子进程拉起，只负责检测剪贴板变化并以 `CHANGE <base64>` 输出到标准输出；解析、去重、归档全部在 Node 侧完成

---

## 快速开始

### 环境要求
- Windows
- Node.js（建议 18+）
- PowerShell（剪贴板传感器依赖）

### 1. 首次配置

```powershell
cd C:\Users\Lenovo\v3
npm install          # 安装依赖，包含 @deepseek-ai/dsh
copy config.example.json config.json
```

> DSH Agent 默认通过 `npm install` 安装的 `@deepseek-ai/dsh` 运行；也可以手动设置 `DSH_BIN` 指向本机 DSH 的 `lib/bin.js`。

也可以不手动编辑，直接启动后在看板里配置：

- **模型服务（Agent 引擎）**：左下角「设置」→ 选服务商、填 API Key / 模型、测试连接
- **剪贴板捕获 / 我的微信昵称**：「记忆 → 记忆输入」（默认关闭，需手动开启）
- **主动级别 / 心跳间隔 / 提醒策略**：「主动 → 策略配置」

### 2. 启动桌面窗口 App（推荐）

```powershell
cd C:\Users\Lenovo\v3\app
npm install   # 首次运行或 node_modules 不存在时需要
npm start
```

启动后会自动拉起核心服务并打开「小鹈鹕」桌面窗口，默认进入空的新对话。

### 3. 开发模式 / 备用启动

```powershell
npm run daemon   # 守护核心：服务 + 剪贴板监听 + 微信通道 + 定时调度
npm run server   # 只看板，浏览器打开 http://127.0.0.1:18791
npm test         # 单元测试与 fixture 测试
npm run check    # 全量语法检查

# 或一键脚本（Edge 应用窗口，等价于 start.ps1）
powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\Lenovo\v3\start.ps1
```

> 剪贴板捕获默认关闭，需在「记忆 → 记忆输入」开启；微信消息收发需先在设置/记忆输入里扫码登录。

### 4. 打包桌面安装包

```powershell
cd C:\Users\Lenovo\v3\app
npm run dist
# 安装包生成在 app\dist\ 下
```

---

## 数据与隐私

- **默认本地优先**：聊天记录默认保存在本地 `C:\Users\Lenovo\v3`，不上传、不外发。
- **剪贴板隐私**：只识别“聊天记录格式”，非聊天内容（密码/验证码/普通文本）立即丢弃、不落盘。
- **Git 安全**：真实联系人档案、聊天流水、意图库、`config.json`（含 Provider Key）、`config.toml`（微信凭据）、未读/对话状态均已被 `.gitignore` 排除，不会提交到仓库。
- **Agent 事件隐私**：工具结果默认截断，历史会话只保留最终回答与执行摘要，避免把完整本地文件内容写入仓库或聊天记录。
- **用户控制**：联系人页可编辑/删除画像，可清空聊天记录；看板会话也可直接删除。

---

## 路线图

### 当前已落地（2026-08）
- ✅ 五模块框架重构 + 引擎直连大模型 API（多 Provider 可配置）
- ✅ DSH Agent 对话回复：所有通道对话自动走 DSH harness，WebUI 流式展示思考/工具/执行过程
- ✅ Agent 事件裁剪、增量 `afterSeq` 拉取、回答卡片执行过程折叠
- ✅ 联系人档案编辑 / 清空聊天记录 / 删除联系人（同步清理 `inbox.jsonl`）
- ✅ 微信 iLink 双向通道（扫码登录 + 长轮询）
- ✅ 看板板块化重构（顶部状态栏 / 对话 / 四大板块 / 子页导航）
- ✅ 待确认行动 + 思考和行动记录 + 全局消息检索
- ✅ 进程内主动提醒调度 + 心跳间隔可配置

### MVP 下一阶段
- 意图识别效果调优：置信度阈值与反馈回流
- 群聊消息模型设计
- 语音回填迁移到 Node 捕获管道
- 知识库 / MCP / Skill / Workflow 落地

### V1.1
- 关系健康度可视化
- PC 端智能回复建议
- 移动端推送

### V1.2
- 图片/多模态理解
- 输入法建议回复
- 多平台接入扩展
- 关系知识图谱 / 情感分析进阶

---

## 相关文档

- [产品设计](产品设计.md) —— 完整产品定位、架构、路线图
- [ARCHITECTURE.md](ARCHITECTURE.md) —— 五模块架构与看板结构
- [RULES.md](RULES.md) —— 归档与数据处理规则

---

## GitHub 项目描述（About 用）

> **小鹈鹕 AI（TinyPelican）**：一个“长记忆 + 主动关系”的个人 AI 助手。复制微信聊天记录即可自动归档联系人、提取任务与 DDL、主动提醒并维护关系，做你时刻在场的第二大脑。
